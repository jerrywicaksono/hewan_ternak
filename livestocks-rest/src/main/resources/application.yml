server:
   port: 9090
   servlet:
    context-path: /livestocks-rest
   http2:
      enabled: false
      
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hewan_ternak
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update